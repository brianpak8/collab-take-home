function Header(props) {
  return (
    <div>
    <h2 className="header">Collab Checker</h2>
    <div className="instructions">Upload 2 CSV files to check for any discrepancies.  Use the "Concern" buttons to limit what categories are compared.</div>
    </div>
  )
}

export default Header;
